import {serverToStock} from "/stocks/helpers.js";

/** @param {NS} ns */
export async function main(ns) {
	while(true) {
		// For each stock we own
		// ignore if we're making money off the server directly
		// weaken + either hack or grow
		// loop
		ns.sleep(1000);
	}
}
